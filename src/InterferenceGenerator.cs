class InterferenceGenerator
{
    public void InsertErrorManually(BinaryString input)
    {
        // length of message has to stay the same as string as immutable
    }

    public void InvertBit(Packet input, int index)
    {
        input.Content.InvertBit(index);
    }
}
